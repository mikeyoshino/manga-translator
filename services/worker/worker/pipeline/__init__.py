"""
Pipeline package — transitional wrapper.

During the migration, the actual pipeline code lives in manga_translator/.
This package exists so future imports like `from worker.pipeline.xxx` will work
once code is moved here in Phase 7.
"""
